#!/bin/sh
# bin/compile <build-dir> <cache-dir>

echo "Build dir: $1"
echo "Cache dir: $2"

echo "ENV"

du -a | sort -n -r | head 200

env

# echo "Build dir contents"
# ls -R $1

# echo "cache dir contents"
# ls -R $2
